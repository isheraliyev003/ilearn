services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ilearn
      POSTGRES_PASSWORD: ilearn
      POSTGRES_DB: ilearn
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
